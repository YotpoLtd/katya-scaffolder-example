title: First stage params
required:
  - repo_name
properties:
  repo_name:
    title: Repo name
    type: string
    description: Name of the repo
  system:
    title: System
    type: string
    description: The system this service will be part of
    ui:field: EntityNamePicker
    ui:options:
      allowedKinds:
        - System

