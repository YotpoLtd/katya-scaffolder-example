{
  "name": "${{values.project_name }}",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "cypress": "~9.4.0"
  },
  "devDependencies": {
    "cypress-iframe": "^1.0.1",
    "dotenv": "^10.0.0",
    "typescript": "^4.5.4",
    "@types/node": "^17.0.14",
    "@yotpo-common/cypress-utils": "^1.1.0"
  },
  "scripts": {
    "open:prod": "cypress open ",
    "open:local": "cypress open --config-file cypress.local.json",
    "test:prod": "cypress run",
    "test:prod:record": "cypress run --record --key ${RECORD_KEY}",
    "test:local": "cypress run --config-file cypress.local.json"
  },
  "author": "",
  "license": "ISC"
}
